import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: 100,
    flexDirection: 'row'
  },
  publisher: {},
  subscriber: {}
});
export default styles;
